print(''' ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~''')
print('''
                                                  BOA NOITE À TODOS
                                                  
''') 


'''if (tabela_especialidades['ID'] == pedemedico).all():
                print('dentro if')

                ID = tabela_especialidades.loc[tabela_especialidades['ID'] == pedemedico , 'ID'].values[0]
                MEDICO = tabela_especialidades.loc[tabela_especialidades['ID'] == pedemedico , 'MÉDICOS'].values[0]
                CONSULTORIO = tabela_especialidades.loc[tabela_especialidades['ID'] == pedemedico, 'CONSULTÓRIO'].values[0]
                DATA = tabela_especialidades.loc[tabela_especialidades['ID'] == pedemedico, 'DATA'].values[0]
                HORA = tabela_especialidades.loc[tabela_especialidades['ID'] == pedemedico, 'HORA'].values[0]
                ESPECIALIDADE = tabela_especialidades.loc[tabela_especialidades['ID'] == pedemedico, 'ESPECIALIDADE'].values[0]
                guardar_id = ID
                guardar_medico = MEDICO
                guardar_consultorio = CONSULTORIO
                guardar_data = DATA
                guardar_hora = HORA
                guardar_especialidade = ESPECIALIDADE
                # esp = [f'{guardar_medico}, {guardar_consultorio}, {guardar_data}, {guardar_hora}, {guardar_hora}']
                print(f'Deseja marcar uma consulta de especialidade {guardar_especialidade} com o médico(a) {MEDICO}, localizado em {CONSULTORIO}\nDATA: {guardar_data:.10} Hora: {guardar_hora} \n\nDIGITE 3 PARA CONFIRMAR DADOS! ')
                esp.append(f'Especialidade: {guardar_especialidade}, Médico: {guardar_medico}, Consultório: {guardar_consultorio}, DATA: {guardar_data:.10}, HORA: {guardar_hora}')
                global teste
                teste = esp'''